{% extends 'base.html.twig' %}

{# ... #}

{% block body %}
<div class="container mt-5 mb-5">
    <h1>Fomulaire de connexion</h1>
        <p class="mb-5 text-primary"><i>Veuillez utiliser ce formulaire pour vous connecter</i></p>

        {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}

    <form action="{{ path('app_login') }}" method="post">
        <label for="username">Votre Email:</label>
        <input class="form-control mb-3 type="text" id="username" name="_username" value="{{ last_username }}" required>

        <label for="password">Votre mot de passe:</label>
        <input class="form-control" type="password" id="password" name="_password" required>

        {# If you want to control the URL the user is redirected to on success #}
        <input type="hidden" name="_target_path" value="{{ path('app_account')}}">

        <button class="btn btn-primary mt-5" type="submit">Me connecter</button>
    </form>

    </br>
    Vous n'avez pas un compte ? <a href="{{ path('app_register')}}">inscrivez-vous </a>
</div>
{% endblock %}
